#Understanding better Tuples -> a finite ordered list that usually cannot be changed
# good for information that doesn´t change (immutable in a sense)
print("Beginnings with tuples")

coordinates = (4, 5) #Initializing a tuple
print(coordinates[0]) #prints the first element
